export interface ReviewFormData {
  rating: number;
  name: string;
  comment: string;
  // Add any additional fields that your review form might need
  // For example:
  // userId?: string;
  // packageId?: string;
  // createdAt?: Date;
}
